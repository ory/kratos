{
  "private": true,
  "scripts": {
    "format": "prettier --single-quote --trailing-comma none --write \"{cypress}/**/*.js\"",
    "test": "cypress run",
    "test:watch": "cypress open",
    "wait-on": "wait-on"
  },
  "devDependencies": {
    "cypress": "4.3.0",
    "prettier": "2.0.4",
    "wait-on": "4.0.2"
  }
}
