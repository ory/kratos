{
  "private": true,
  "scripts": {
    "format": "prettier --no-semi --single-quote --trailing-comma es5 --write \"e2e/test/cypress/**/*.js\"",
    "test": "cypress run",
    "test:watch": "cypress open",
    "wait-on": "wait-on"
  },
  "devDependencies": {
    "cypress": "4.5.0",
    "prettier": "2.0.4",
    "wait-on": "4.0.2",
    "openapi-generator": "^0.1.36"
  }
}
