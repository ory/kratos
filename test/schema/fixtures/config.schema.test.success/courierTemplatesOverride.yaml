courier:
  templates:
    recovery:
      valid:
        email:
          body:
            plaintext: "base64://"
            html: "base64://"
          subject: "Recovery Email"
      invalid:
        email:
          body:
            plaintext: "base64://"
            html: "base64://"
          subject: "Recovery Email"
  template_override_path: "/some/path"
