selfservice:
  default_browser_return_url: "#/definitions/defaultReturnTo"
  flows:
    verification:
      enabled: true

dsn: foo

identity:
  schemas:
    - id: default
      url: https://example.com
