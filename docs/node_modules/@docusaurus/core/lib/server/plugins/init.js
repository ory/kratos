"use strict";
/**
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const lodash_1 = __importDefault(require("lodash"));
const import_fresh_1 = __importDefault(require("import-fresh"));
function initPlugins({ pluginConfigs, context, }) {
    const plugins = lodash_1.default.compact(pluginConfigs.map(pluginItem => {
        let pluginModuleImport;
        let pluginOptions = {};
        if (!pluginItem) {
            return null;
        }
        if (typeof pluginItem === 'string') {
            pluginModuleImport = pluginItem;
        }
        else if (Array.isArray(pluginItem)) {
            pluginModuleImport = pluginItem[0];
            pluginOptions = pluginItem[1] || {};
        }
        if (!pluginModuleImport) {
            return null;
        }
        // The pluginModuleImport value is any valid
        // module identifier - npm package or locally-resolved path.
        const pluginModule = import_fresh_1.default(pluginModuleImport);
        return (pluginModule.default || pluginModule)(context, pluginOptions);
    }));
    return plugins;
}
exports.initPlugins = initPlugins;
