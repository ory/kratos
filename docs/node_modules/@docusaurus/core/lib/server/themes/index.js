"use strict";
/**
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
Object.defineProperty(exports, "__esModule", { value: true });
const alias_1 = require("./alias");
function loadThemeAlias(themePaths, userThemePaths = []) {
    const aliases = {};
    themePaths.forEach(themePath => {
        const themeAliases = alias_1.themeAlias(themePath);
        Object.keys(themeAliases).forEach(aliasKey => {
            aliases[aliasKey] = themeAliases[aliasKey];
        });
    });
    userThemePaths.forEach(themePath => {
        const userThemeAliases = alias_1.themeAlias(themePath, false);
        Object.keys(userThemeAliases).forEach(aliasKey => {
            aliases[aliasKey] = userThemeAliases[aliasKey];
        });
    });
    return aliases;
}
exports.loadThemeAlias = loadThemeAlias;
